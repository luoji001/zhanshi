 1.这是一个给展示数据信息的网站，数据信息来自于本地excel文件的解析，excel中有的数据全部展示
  2.这个项目是一个对外展示窗口，UI设计要求干净、简洁、易读性高、对互联网用户友好
  3.页面渲染仍然全部是构建期静态产物（next.config.ts 的 output: 'export' 未变）。运行时只有
    worker/index.ts 这一段边缘代码，且只做两件事：给页面访问记 PV/UV、提供站长自用的 /api/stats。
    它**不参与渲染、不改动页面内容**；加任何会影响页面输出的运行时逻辑，都属于改变本项目定位，先问