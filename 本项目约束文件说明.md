# 本项目约束文件说明

**本文件只做索引，不复述任何规则。**

这里回答：**改这个项目时，哪些文件在管我、各自管到哪、哪些看着像其实不算。**

⚠️ **「在管我」取决于你此刻用哪个工具。** Claude Code 读什么、opencode 读什么是两件事，
所以下面每张表都带一列「谁加载」—— 不要把它合并成一个笼统的「生效」。

> **本项目 2026-09-21 从 `information-github` 仓库迁出**，现位于 `/Users/luoji/information-haiwai/next-zhanshi/`。
> **尚未纳入任何 git 仓库**，所以「随仓库分发」这一层**目前是空的** —— 既没有仓库级 `AGENTS.md`，
> 也没有版本控制。代码历史仍留在 `information-github` 仓库（迁出前最后一次提交 `d6c21aa7` 有完整目录树，
> 可用 `git show d6c21aa7:next-zhanshi/<路径>` 取回）。

## 甲组：跟着项目走

| # | 文件 | 层级 | 管什么 | 谁加载 |
| --- | --- | --- | --- | --- |
| 1 | `AGENTS.md`（本目录） | **项目级入口** | 项目定位：本站是纯前端展示站，只做一件事 —— 把本地 Excel 里的数据原样渲染到页面上。**本项目结论以它为准** | Claude Code ✅、opencode ✅ |
| 2 | `src/**`、`scripts/**` 各文件里的注释 | 项目内依据（**分散**） | 局部实现约定与实测值（零依赖解析器为何「宁可报错」、列口径与字段清单、sticky 表头为何要 `border-collapse: separate`、价格列为何跟着右对齐、标题断行、对比度实测值）—— 不集中收录，改前读一眼 | ⛔ 无工具加载，靠人 / agent 主动读 |
| 3 | `本项目约束文件说明.md`（本文件） | 索引 | 只登记「哪些文件在管我」，本身不设约束 | ⛔ 同上 —— **看着像约束，其实不算** |

读法：#1 给结论，#2 给依据。

**迁出时丢掉的一层**：`information-github` 仓库根的 `AGENTS.md` 曾作为本项目的「仓库级」约束
（目录职责 / 前后端对应 / 通用约定 / 静态校验命令）。它在 `/Users/luoji/information-github/AGENTS.md`，
**属于那个仓库、只管那个仓库里的项目，现在不管本目录**。本目录的 `../AGENTS.md` 不存在。

> **已删除的两份项目内依据**：`docs/ui.md` 与 `docs/data-source.md` 都是 0 字节空文件，内容早已丢失，已随「只保留 Excel 数据渲染」的瘦身一并删除（`docs/` 目录不再存在）。原表格里指向它们的两行相应移除，代码注释里的失效指针也已就地改写 —— 该保留的规则（配色只走 `globals.css` 令牌、文字底色组合过 WCAG AA 4.5:1、列口径与字段清单）现在直接写在对应源码的注释里。

## 乙组：本机用户级（跟机器走，不跟仓库走）

这一层**搬到哪儿都在**，但只对装了对应工具的机器生效。

### B1 · Claude Code 的用户级

| 文件 | 管什么 | 谁加载 |
| --- | --- | --- |
| `~/.claude/settings.json` | 权限模式（`bypassPermissions`）、模型与 effort、`skillOverrides`、10 个 hook 事件全挂 iTerm2 状态脚本 | Claude Code ✅ |
| `~/.claude/CLAUDE.md`、`~/.claude/AGENTS.md` | — | **不存在** —— 所以 Claude Code 在本项目只吃甲组 #1 |
| `~/.claude/projects/<slug>/memory/MEMORY.md` | 记忆层 | 目录为空，**当前零约束**。slug 由 cwd 决定，迁出后换成新 slug（本机已出现 `-Users-luoji-information-haiwai-sh`） |
| `~/.claude/commands/sessions.md` | 自定义斜杠命令 `/sessions`（`disable-model-invocation: true`） | Claude Code ✅ —— 是能力，不是约束 |
| `~/.claude/plugins/` | 插件 | 只有 marketplace 缓存，**未装任何插件** |

### B2 · opencode 的用户级 —— 本目录下**唯一的第三方规则**

| 文件 | 管什么 | 谁加载 |
| --- | --- | --- |
| `~/.config/opencode/AGENTS.md` | opencode 用户级：中文回复、`session-*.md` 存在时提示 `/move-export`、`CREATE TABLE` 必须带表/字段注释 | opencode ✅ —— **Claude Code ⛔** |
| `~/.config/opencode/opencode.jsonc` | opencode 用户级**硬拦截**：`rm -rf *`、`git push*`、`git reset --hard*`、`git clean -fd*`、`chmod -R *`、`sudo *` 全 deny | opencode ✅ —— **Claude Code ⛔** |

## 丙组：留在 `information-github` 仓库，**已不再管本目录**

这一组曾经在本项目生效（项目当时在那个仓库里）。**迁出后与本目录无关** ——
关键在 `opencode.json` 的 `instructions` 里的路径是**相对仓库根**解析的，在新位置挂不上。

| 文件（均在 `/Users/luoji/information-github/`） | 现在归谁 | 谁加载 |
| --- | --- | --- |
| `.cursor/rules/engineering-standards.mdc`（`alwaysApply: true`、globs `**/*`） | 只管那个仓库里的项目 | opencode ✅、Cursor ✅ —— Claude Code ⛔ |
| `.cursor/rules/ui-standards.mdc`、`.cursor/rules/product-doc-standards.mdc` | 同上 | 同上 |
| `opencode.json`（已入库） | 同上 | opencode ✅ —— Claude Code ⛔ |
| `.opencode/skills/*`（11 条软链接） | 同上 | opencode ✅ —— Claude Code ⛔ |
| `.claude/settings.local.json` | 同上 | ⚠️ 未确证 |

⚠️ **迁出带来的实际后果**：那三份 mdc 里有一份 `engineering-standards.mdc` 是 `alwaysApply: true`、
glob `**/*`，在旧位置「任何任务都读」。**在本目录，opencode 和 Cursor 现在都读不到它们了** ——
本目录下没有任何第三方规则文件，只剩甲组 #1 那份 `AGENTS.md`。

判定「Claude Code ⛔」的依据：Claude Code 侧**没有任何地方引用它们** —— 用户级 `settings.json`
无 `instructions` 字段、本目录无 `CLAUDE.md`、本目录下无 `.claude/`。

### 两条后果，别踩

1. **旧规矩「`alwaysApply: true` 任何任务都读」在本目录已失效。** 别照旧记忆行事。
2. **opencode 的硬拦截在 Claude Code 这边是反的。** opencode 拦 `rm -rf *` / `git push*` / `git reset --hard*`；而 `~/.claude/settings.json` 是 `bypassPermissions` + `skipDangerousModePermissionPrompt: true`。**同一台机器，一套上锁一套全开** —— 换个工具，危险操作的门槛完全不同。

**优先关系未定义**：没有任何文件规定乙组、丙组与甲组谁优先。真撞上时不要自己拍板，问用户。

## 维护

- 新增 / 删除 / 改名本目录的约束文件，**同步改本表**；表里漏了文件，这份说明就失去意义。
- 只写「哪个文件管什么、**谁加载它**」，**不抄规则正文** —— 抄了就会出现两处各存一份然后漂移。
- **本目录一旦建立 git 仓库或在 `information-haiwai/` 下新增仓库级 `AGENTS.md`，本表要重排** ——
  甲组会多出一层，丙组可能部分回流。
- **「谁加载」这一列会随工具版本变**：某个工具读不读 `AGENTS.md`、认不认 `.cursor/rules`，是**产品行为**，不是本目录的事实。要改这一列，**先实测**，别照抄。
