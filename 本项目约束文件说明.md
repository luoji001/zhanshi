# 本项目约束文件说明

**本文件只做索引，不复述任何规则。**

这里回答：**改这个项目时，哪些文件在管我、各自管到哪、哪些看着像其实不算。**

⚠️ **「在管我」取决于你此刻用哪个工具。** Claude Code 读什么、opencode 读什么是两件事，
所以下面每张表都带一列「谁加载」—— 不要把它合并成一个笼统的「生效」。

> **本项目 2026-09-21 从 `information-github` 仓库迁出**，现位于 `/Users/luoji/information-haiwai/next-zhanshi/`。
> **同日已建立独立 git 仓库**：远端 `origin` = `https://github.com/luoji001/zhanshi.git`，现有 6 个提交
> （最新 `373c56f`）。所以「随仓库分发」这一层**现在是实的** —— 但**只有一层**：本目录是单项目仓库，
> 仓库根 = 项目根，根目录那份 `AGENTS.md` 同时充当仓库级与项目级（见甲组 #1）。丙组**没有回流** ——
> `information-github/opencode.json` 的 `instructions` 路径相对那个仓库根解析，在新位置挂不上。
>
> **历史是重建的，不是续接的**：新仓库的初始提交 `df15bb9`「初始提交：zhanshi 纯静态展示站」，
> 不是从 `d6c21aa7` 续下来的。迁出前的历史仍在 `information-github` 仓库（那次提交有完整目录树，
> 可用 `git show d6c21aa7:next-zhanshi/<路径>` 取回）—— 该路径**仍可用，但已不是本目录唯一的来源**。

## 甲组：跟着项目走

| # | 文件 | 层级 | 管什么 | 谁加载 |
| --- | --- | --- | --- | --- |
| 1 | `AGENTS.md`（本目录） | **项目级入口**（**同时是仓库级** —— 单项目仓库，两层是同一个文件） | 项目定位：本站是纯前端展示站，只做一件事 —— 把本地 Excel 里的数据原样渲染到页面上。**本项目结论以它为准** | Claude Code ✅、opencode ✅ |
| 2 | `src/**`、`scripts/**`、`worker/**`、`wrangler.jsonc` 各文件里的注释 | 项目内依据（**分散**） | 局部实现约定与实测值（零依赖解析器为何「宁可报错」、列口径与字段清单、sticky 表头为何要 `border-collapse: separate`、价格列为何跟着右对齐、标题断行、对比度实测值、`overflow: clip` 为何不能写回 `hidden`、统计为何在边缘记而非埋 beacon、`run_worker_first` 为何必须是 glob 数组而不是 `true`）—— 不集中收录，改前读一眼 | ⛔ 无工具加载，靠人 / agent 主动读 |
| 3 | `本项目约束文件说明.md`（本文件） | 索引 | 只登记「哪些文件在管我」，本身不设约束 | ⛔ 同上 —— **看着像约束，其实不算** |

读法：#1 给结论，#2 给依据。

**甲组之外的项目根文件，不构成约束**：`commit`（构建校验 + 提交 + 推送的脚本，行为见下方「三条后果」#3）、
`next.config.ts`、`package.json`、`tsconfig.json`、`README.md` —— 是配置、工具与使用说明，
不是「在管我」的规则。**别往甲组里塞**，否则本表就从「约束清单」变成「文件清单」。

> `README.md` 只讲操作层（怎么跑、怎么改数据、怎么发、密钥有哪些），并且**指向**甲组 #1 与本文件，
> 不复述规则 —— 与本文件的定位不冲突：它是「怎么用」，本文件是「哪些文件在管我」。

> ⚠️ **`next.config.ts` 是反例式的例外，值得看一眼**：它本身不设约束，但那段注释解释
> 「加动态能力**不一定**要拿掉 `output: 'export'`」—— 统计 Worker 就是反例。
> 不知道这一条，很容易以为要加运行时就得推翻整个静态导出方案。
>
> **`wrangler.jsonc` 已从上面这份「不构成约束」名单里移走，升进甲组 #2**：加统计之前它确实
> 只是一段托管配置，现在它的注释里带着两条承重约定（`run_worker_first` 必须用 glob 数组而非
> `true`、以及 `assets.binding` 为何必须有），已经是「改前读一眼」的地方了。

**迁出时丢掉的一层**：`information-github` 仓库根的 `AGENTS.md` 曾作为本项目的「仓库级」约束
（目录职责 / 前后端对应 / 通用约定 / 静态校验命令）。它在 `/Users/luoji/information-github/AGENTS.md`，
**属于那个仓库、只管那个仓库里的项目，现在不管本目录** —— 该文件自己列的「有专属 `AGENTS.md` 的项目」
名单里也只有 `next-information-github/` 与 `fast-api-information-github/`，没有本项目。本目录的
`../AGENTS.md`（即 `information-haiwai/AGENTS.md`）不存在；本目录**自己的**仓库级入口就是甲组 #1。

> **已删除的两份项目内依据**：`docs/ui.md` 与 `docs/data-source.md` 都是 0 字节空文件，内容早已丢失，已随「只保留 Excel 数据渲染」的瘦身一并删除（`docs/` 目录不再存在）。原表格里指向它们的两行相应移除，代码注释里的失效指针也已就地改写 —— 该保留的规则（配色只走 `globals.css` 令牌、文字底色组合过 WCAG AA 4.5:1、列口径与字段清单）现在直接写在对应源码的注释里。

## 乙组：本机用户级（跟机器走，不跟仓库走）

这一层**搬到哪儿都在**，但只对装了对应工具的机器生效。

### B1 · Claude Code 的用户级

| 文件 | 管什么 | 谁加载 |
| --- | --- | --- |
| `~/.claude/settings.json` | 权限模式（`bypassPermissions`）、模型与 effort、`skillOverrides`、10 个 hook 事件全挂 iTerm2 状态脚本 | Claude Code ✅ |
| `~/.claude/CLAUDE.md`、`~/.claude/AGENTS.md` | — | **不存在** —— 所以 Claude Code 在本项目只吃甲组 #1 |
| `~/.claude/projects/<slug>/memory/MEMORY.md` | 记忆层 | 记忆目录**为空**，**当前零约束**（`MEMORY.md` 不存在）。slug 由 cwd 决定，**本项目自己的 slug 有两个**：`-Users-luoji-information-github-next-zhanshi`（迁出前，历史最全）、`-Users-luoji-information-haiwai-next-zhanshi`（**当前**）—— 两者记忆互不相通。同层另有 `-Users-luoji-information-haiwai`、`-Users-luoji-information-haiwai-sh`，属父目录与兄弟目录，与本项目无关 |
| `~/.claude/commands/sessions.md` | 自定义斜杠命令 `/sessions`（`disable-model-invocation: true`） | Claude Code ✅ —— 是能力，不是约束 |
| `~/.claude/plugins/` | 插件 | 只有 marketplace 缓存，**未装任何插件** |

### B2 · opencode 的用户级 —— 本目录下**唯一的第三方规则**

| 文件 | 管什么 | 谁加载 |
| --- | --- | --- |
| `~/.config/opencode/AGENTS.md` | opencode 用户级：中文回复、`session-*.md` 存在时提示 `/move-export`、`CREATE TABLE` 必须带表/字段注释 | opencode ✅ —— **Claude Code ⛔** |
| `~/.config/opencode/opencode.jsonc` | opencode 用户级**硬拦截**：`rm -rf *`、`git push*`、`git reset --hard*`、`git clean -fd*`、`chmod -R *`、`sudo *` 全 deny | opencode ✅ —— **Claude Code ⛔** |

### B3 · `~/.agents/` —— **看着像约束，其实不算**

| 文件 | 管什么 | 谁加载 |
| --- | --- | --- |
| `~/.agents/skills/**`（含 `.skill-lock.json` 版本锁） | **技能库，不是规则文件** —— 装的是能力（`archify` 等第三方 skill），不规定你「必须怎么写」 | ⛔ 无工具直接加载 |
| `~/.agents/_agents.mount_config` | 容器挂载映射（`.../.agents → /mnt/.agents`），与规则无关 | ⛔ |

⚠️ **它挂在 opencode 侧，不在 Claude Code 侧**：`~/.config/opencode/opencode.jsonc` 的 `external_directory`
显式放行 `~/.agents/**`；而 `~/.claude/skills` **不存在**（没有软链把它挂进 Claude Code）。
所以在这台机器上，**它不构成对本项目的约束**，别按「装了 skill 就生效」的直觉行事。

## 丙组：留在 `information-github` 仓库，**已不再管本目录**

这一组曾经在本项目生效（项目当时在那个仓库里）。**迁出后与本目录无关** ——
关键在 `opencode.json` 的 `instructions` 里的路径是**相对仓库根**解析的，在新位置挂不上。

| 文件（均在 `/Users/luoji/information-github/`） | 现在归谁 | 谁加载 |
| --- | --- | --- |
| `.cursor/rules/engineering-standards.mdc`（`alwaysApply: true`、globs `**/*`） | 只管那个仓库里的项目 | opencode ✅、Cursor ✅ —— Claude Code ⛔ |
| `.cursor/rules/ui-standards.mdc`、`.cursor/rules/product-doc-standards.mdc` | 同上 | 同上 |
| `opencode.json`（已入库） | 同上 | opencode ✅ —— Claude Code ⛔ |
| `.opencode/skills/*`（11 条软链接） | 同上 | opencode ✅ —— Claude Code ⛔ |
| `.claude/settings.local.json` | 同上 | ⚠️ 未确证 |

⚠️ **迁出带来的实际后果**：那三份 mdc 里有一份 `engineering-standards.mdc` 是 `alwaysApply: true`、
glob `**/*`，在旧位置「任何任务都读」。**在本目录，opencode 和 Cursor 现在都读不到它们了** ——
本目录下没有任何第三方规则文件，只剩甲组 #1 那份 `AGENTS.md`。

判定「Claude Code ⛔」的依据：Claude Code 侧**没有任何地方引用它们** —— 用户级 `settings.json`
无 `instructions` 字段、本目录无 `CLAUDE.md`、本目录下无 `.claude/`。

### 三条后果，别踩

1. **旧规矩「`alwaysApply: true` 任何任务都读」在本目录已失效。** 别照旧记忆行事。
2. **opencode 的硬拦截在 Claude Code 这边是反的。** opencode 拦 `rm -rf *` / `git push*` / `git reset --hard*`；而 `~/.claude/settings.json` 是 `bypassPermissions` + `skipDangerousModePermissionPrompt: true`。**同一台机器，一套上锁一套全开** —— 换个工具，危险操作的门槛完全不同。
3. **本目录根部的 `commit` 是可执行脚本，一次跑完「暂存 → 生产构建校验 → commit → `git push` → 触发线上发布」**（`COMMIT_SKIP_CHECK=1` 可跳过校验）。它同时踩在上面两条上：**opencode 侧 deny 的 `git push*`，在这条脚本里是正常路径**；而它 push 之后 Cloudflare 会自动构建上线，**推送即对外发布**。别顺手跑它，也别以为「本地跑过 dev 就没问题」—— 校验那一步跑的才是与线上一致的构建。

**优先关系未定义**：没有任何文件规定乙组、丙组与甲组谁优先。真撞上时不要自己拍板，问用户。

## 维护

- 新增 / 删除 / 改名本目录的约束文件，**同步改本表**；表里漏了文件，这份说明就失去意义。
- 只写「哪个文件管什么、**谁加载它**」，**不抄规则正文** —— 抄了就会出现两处各存一份然后漂移。
- **本目录建立 git 仓库这一条已于 2026-09-21 发生，表已按此重排**：因为是单项目仓库（仓库根 = 项目根），
  甲组**没有多出一层**，只是 #1 升格为「项目级 + 仓库级同一个文件」；丙组**没有回流**。
  下一个触发条件只剩一条 —— **在 `information-haiwai/` 下新增仓库级 `AGENTS.md`**，届时甲组才真的会多一层。
- **「谁加载」这一列会随工具版本变**：某个工具读不读 `AGENTS.md`、认不认 `.cursor/rules`，是**产品行为**，不是本目录的事实。要改这一列，**先实测**，别照抄。
