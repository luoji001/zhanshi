/**
 * **静态文案**的单一事实来源：三个区块的标题与说明文字。
 * 改文案只需改这里，不用进组件。
 *
 * ⚠️ 名录**数据**不在这里 —— 它来自 src/excel/源头厂商.xlsx，见 ./suppliers.ts。
 * 本文件必须保持**客户端可用**（将来若有 'use client' 组件引用它），
 * 因此**绝对不要**在这里 import node:fs 或任何服务端专属模块。
 */

/**
 * 仓库地址（2026-09-23 仓库转 public 后新增）。站上有三处要用：
 * 「数据来源」的源文件链接、页脚的源文件链接与「数据勘误」入口。
 * 统一从这里取，**不要在组件里硬编码 URL**；换仓库只改这里。
 *
 * ⚠️ sourceUrl 里的中文文件名必须 URL 编码（本文件里写的是编码后的形式），
 * 否则部分客户端会把链接拼错。仓库默认分支是 main，换分支要一起改。
 */
export const repo = {
  url: 'https://github.com/luoji001/zhanshi',
  /** 源文件在 GitHub 上的可读页面。它公开可查是本站「可自行核对」这一承诺的全部依据 */
  sourceUrl:
    'https://github.com/luoji001/zhanshi/blob/main/src/excel/%E6%BA%90%E5%A4%B4%E5%8E%82%E5%95%86.xlsx',
  /** 数据勘误入口（Issues 已开）。页脚用它做「发现数据有问题能告诉你」的闭环 */
  issuesUrl: 'https://github.com/luoji001/zhanshi/issues/new',
  /**
   * 源文件的公开提交历史。它本身就是一份「谁在何时改了什么」的变更记录，
   * 零成本、无需维护 —— 前提是数据更新时写有意义的提交信息（见 README）。
   */
  commitsUrl:
    'https://github.com/luoji001/zhanshi/commits/main/src/excel/%E6%BA%90%E5%A4%B4%E5%8E%82%E5%95%86.xlsx',
} as const;

/**
 * 站点外壳（顶栏 / 页脚）的文案与导航。
 *
 * ⚠️ 2026-09-22 新增，**推翻了此前「全站没有导航栏」那条约定**。旧约定的理由是
 * 「两页都很短，页内跳转够了」，但那样一来两页各自看都成立、合起来看却不像一个站：
 * 从搜索引擎直接落到 /data 的人，在页面上找不到任何「这是哪个站」的线索。
 * 现在两页之间的通路是：顶栏（常驻）+ 页脚 + 首页按钮。
 *
 * 站点名放在这里做**单一事实来源**：顶栏品牌、页脚品牌、<title> 与 og:site_name
 * （见 app/layout.tsx）四处都从 site.name 取，不要在别处再抄一遍字面量。
 */
export const site = {
  /**
   * 站点正式地址。og:image 的绝对地址（layout.tsx 的 metadataBase）、
   * robots/sitemap、JSON-LD、引用格式都从这里取 —— **换域名只改这一处**
   * （另需同步 wrangler.jsonc 的 name）。
   */
  url: 'https://zhanshi.liangpengzhan.workers.dev',
  name: '源头厂商名录',

  // 顶栏导航。全站就两个页面，两条即全部；不做「当前页高亮」（原因见 SiteHeader.tsx）
  nav: [
    { href: '/', label: '概览' },
    { href: '/data', label: '名录数据' },
  ],

  /**
   * 页脚导航 = 顶栏两条 + 「数据说明」锚点 + 「数据勘误」外部入口。
   * 锚点只进页脚不进顶栏：顶栏那两条要维持「这个站有两个页面」的结构感，
   * 混进一个 /data 内部的锚点会把它搅浑。
   *
   * ⚠️ external 每条都要写（哪怕 false）：SiteFooter 按它决定渲染 <Link> 还是
   * 带 target="_blank" 的 <a>，字段缺省会让 TS 推断出的联合类型取不到该属性。
   */
  footerLinks: [
    { href: '/', label: '概览', external: false },
    { href: '/data', label: '名录数据', external: false },
    { href: '/data#data-source', label: '数据说明', external: false },
    { href: repo.issuesUrl, label: '数据勘误', external: true },
  ],

  /**
   * 页脚文字。**只放站上真实存在的事**（站点名、两页入口、由 Excel 推导的条数、
   * 数据快照日期、公开仓库），**不写公司主体 / 备案号 / 客服电话 / 二维码** ——
   * 本站是纯数据展示页，不承载主体信息，编一个出来就是造假。
   * 页脚里的数字由 SiteFooter.tsx 从 getDirectory() 现算，不在这里写死。
   */
  footer: {
    tagline: '把源头厂商的供货记录整理成一张可检索、可排序的表，全部记录取自同一份源文件。',
    // 「谁在维护」：匿名站天然低信任。如实写个人维护 + 可核对，不写无法自证的声明
    // （如「与厂商无合作关系」）。
    about: '本站由个人维护，数据可对照公开源文件逐条核对。',
    // 隐私说明。三句都必须是真的：页面无 Cookie、无第三方请求（字体已自托管，
    // 见 layout.tsx）、访问统计只存在于边缘（见 worker/index.ts，不落 IP）。
    // ⚠️ 一旦以后加回任何第三方脚本或 Cookie，这句必须同步改，不能留成假话。
    privacy: '页面不设 Cookie、不加载第三方脚本，访问量仅在边缘以匿名方式计数。',
    // 遮罩口径 2026-09-23 改写。此前是「均已遮挡，不提供完整联系方式」——
    // 仓库转 public 后源文件（含完整值）任何人都能拿到，再说得像在保密就是误导。
    // 现在如实说清：遮挡只是页面层的防抓取，完整值在公开源文件里。
    masked: '页面上的联系电话与邮件地址仅做展示遮挡，以减少批量抓取。',
    // 紧跟 masked 渲染的链接，与它连起来是一句完整的话。链接文字自带去处，
    // 不要改成「点击这里」这类对读屏无信息的写法。
    sourceLink: '完整源文件在 GitHub 公开可查',
  },
} as const;

export const overview = {
  // 标题是中性数据站标题。本站只展示 Excel 里的名录数据，不承载公司主体信息。
  // 与顶栏品牌、<title> 共用一个值，改这里四处一起变（见上面 site.name）
  title: site.name,
  // 面向访客写的，不要退回成实现说明：「本地」「构建时解析」这类词对访客没有指代对象，
  // 技术细节归 /data 的「数据说明」（见下面 dataSource.notes 第一条）。
  // 「原样呈现」与「不做增删与修饰」原是同一个意思说两遍，已合并。
  // 「原样呈现，不增删、不加工」已删：联系方式被遮罩、价格做了显示格式化，
  // 那句话不再成立。口徑细节仍在 /data 的数据说明里，这里只留一句站得住的结论。
  lead: '源头厂商的供货记录都在这里，取自同一份公开源文件，记录一条不落；联系方式在页面上做了遮挡。',
  // 首页只有概览，名录与数据说明都在 /data，故这两个 href 是跨页路由而非页内锚点。
  // ⚠️ 2026-09-22 起两页之间不再「只有这两条通路」：顶栏与页脚（见上面 site）
  // 也是常驻通路，它们盖住了原来那条「必须同步」的约束。
  actions: [
    { href: '/data', label: '查看名录数据', primary: true },
    { href: '/data#data-source', label: '数据说明', primary: false },
  ],
  /**
   * 品类索引带的标题。这条带子是 2026-09-22 加的，加它是因为：在此之前首页
   * 只有六个数字和两个按钮，**数字是死的、两个按钮还都指向 /data** ——
   * 「落地页 → 我关心的那一类数据」中间没有任何一步。每粒标签是一个
   * 「进 /data 并只看该品类」的深链（拼链接见 lib/tableParams.ts 的 categoryHref），
   * 一步到位。
   *
   * ⚠️ 它不违反「首页只留概览，名录表格在 /data」那条约定：这里给的是**品类索引**，
   * 不是记录；一条供货记录都不在这条带子上。也没有和「查看名录数据」重复 ——
   * 那两件事前面已经因为重复删过一次（见 overview.actions 的注释）。
   */
  categoryTitle: '覆盖品类',
} as const;

export const directory = {
  title: '名录数据',
  // 同样去掉「Excel」这个技术栈名词（口径细节见 dataSource）；「以下」是指向表格的冗余方位词，
  // 换成实指「都在这张表里」，顺带告诉访客表格就在下面。
  // 可做的四件事：2026-09-22 补了「按品类筛选」（见 DirectoryTable.tsx），文案跟着加上。
  lead: '源文件中的全部记录都在这张表里，可按关键词检索、按厂商与品类筛选、点表头排序。',
  emptyText: '数据源中暂无记录。',
} as const;

export const dataSource = {
  title: '数据说明',
  lead: '本页数据的来源、口径与更新方式。',
  // 字段口径逐条对应 src/lib/suppliers.ts 的 SupplierRow，改口径要两处一起改
  fields: [
    { label: '厂商名称', desc: '数据源「源头厂商」列' },
    { label: '品类', desc: '数据源「品类」列，归类以源文件写法为准，站点不另行归并' },
    { label: '供应货品', desc: '数据源「商品」列' },
    { label: '价格', desc: '数据源「价格」列，按该列自带的美元格式 $#,##0.00 展示，未做汇率换算' },
    { label: '联系电话', desc: '数据源「联系方式」列，只展示前 3 位与后 3 位，中间以 * 遮挡' },
    { label: '邮件地址', desc: '数据源「邮件地址」列，@ 前只保留前 3 位，其余以 * 遮挡；域名原样保留' },
    // 「是否验证」的口径必须写足：站上不做判定，但也**不能只说「原样展示」**——
    // 访客看到「是」会默认理解成「站方核实过」。2026-09-23 实测数据里 58 条「是」、
    // 20 条「否」，说不清验证方就是让站点替数据源背了它背不起的书。
    {
      label: '是否验证',
      desc: '数据源「是否验证」列，原样展示。验证方与判定标准由数据源提供方定义，本站未独立核实，该列不代表本站的认证或背书',
    },
  ],
  /**
   * 说明条目。**顺序是有意的**：先说来源与可核对，再说口径，最后才是免责与质量承诺。
   * 每条都必须是站上真实成立的事 —— 这里不写任何无法自证的套话
   * （「我们致力于……」这类），写了反而伤信任。
   */
  notes: [
    // 可验证性：仓库 public 之后，源文件链接是最硬的信任信号，比自我声明有力
    '数据由源文件在构建时解析生成，页面不连接任何后端接口。源文件在 GitHub 仓库公开可获取（见上方「数据来源」链接），展示内容与源文件逐条对应，可自行核对。',
    '收录的记录一条不落，不做人工筛选；源文件更新后重新构建，页面即同步。',
    '价格按源文件「价格」列自带的美元格式显示（如 $3,999.00），只是显示格式与源文件对齐，数值本身未做汇率换算或任何加工。',
    // 必须说清楚：这是站点对数据源做的**唯一**一处改动。仓库转 public 后不再宣称
    // 「保护隐私」，如实写成页面层的防抓取 —— 完整值在公开源文件里，说错反而失信
    '联系电话与邮件地址在构建时即被遮挡：电话只留前 3 位与后 3 位，邮箱只留 @ 前的 3 位（域名保留）。遮挡只为减少页面层面的批量抓取，完整值可从公开源文件中获取；本站不提供代转或索取渠道。',
    // 免责。B2B 名录没有时效声明是专业缺口，也是实打实的责任边界
    '名录信息会随时间变化，价格与供货情况请以与厂商实际沟通为准；本站不对据此作出的交易决定承担责任。',
    // 把构建期的强校验写成公开承诺。它本来就是真的：列口径见 suppliers.ts 的 mapHeader，
    // 缺列/多列直接抛错让构建失败，解析器另有 scripts/xlsx-selftest.mjs
    '本站每次构建都会强制校验源文件结构：名录固定 7 列，缺少或多出列都会直接构建失败，不会静默上线；Excel 解析器另有独立自检脚本。',
    // 可复现性：仓库公开、构建命令固定，这条声明是能被任何人验证的（不是套话）
    '本页可由公开仓库复现：克隆仓库后执行 npm install 与 npm run build，即可得到与本页内容一致的静态产物。',
  ],

  /** 「数据体检」小节。数字全部由 suppliers.ts 的 deriveQuality 从源文件推导 */
  qualityTitle: '数据体检',
  qualityLead: '以下数字由源文件在构建时计算，数据变化时会随之变化。',

  /**
   * 许可与引用（2026-09-23 新增）。CC BY 4.0 与「署名主体 = 站名」均经维护者确认。
   * 引用格式里的快照日期由 DataSource.tsx 从数据现取，不在这里写死。
   */
  license: {
    label: '许可协议',
    name: 'CC BY 4.0',
    // 指向中文 deed；法律文本仍以英文原件为准
    href: 'https://creativecommons.org/licenses/by/4.0/deed.zh',
    note: '可自由复制、转载、改编，需注明来源为「源头厂商名录」并保留许可链接',
  },
  download: {
    label: '下载数据',
    href: '/data.csv',
    buttonText: '下载完整数据（CSV）',
    note: '与页面内容一致，联系方式同样已遮挡；含源文件行号与数据快照日期。',
  },
  citation: {
    title: '引用格式',
    /** CC BY 要求的署名主体 */
    attribution: '源头厂商名录',
    hint: '引用时请补上访问日期',
  },
  /** 源文件变更历史的展示文案（链接地址在 repo.commitsUrl） */
  historyLabel: '源文件变更历史',
} as const;
