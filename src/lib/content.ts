/**
 * **静态文案**的单一事实来源：三个区块的标题与说明文字。
 * 改文案只需改这里，不用进组件。
 *
 * ⚠️ 名录**数据**不在这里 —— 它来自 src/excel/源头厂商.xlsx，见 ./suppliers.ts。
 * 本文件必须保持**客户端可用**（将来若有 'use client' 组件引用它），
 * 因此**绝对不要**在这里 import node:fs 或任何服务端专属模块。
 */

/**
 * 站点外壳（顶栏 / 页脚）的文案与导航。
 *
 * ⚠️ 2026-09-22 新增，**推翻了此前「全站没有导航栏」那条约定**。旧约定的理由是
 * 「两页都很短，页内跳转够了」，但那样一来两页各自看都成立、合起来看却不像一个站：
 * 从搜索引擎直接落到 /data 的人，在页面上找不到任何「这是哪个站」的线索。
 * 现在两页之间的通路是：顶栏（常驻）+ 页脚 + 首页按钮。
 *
 * 站点名放在这里做**单一事实来源**：顶栏品牌、页脚品牌、<title> 与 og:site_name
 * （见 app/layout.tsx）四处都从 site.name 取，不要在别处再抄一遍字面量。
 */
export const site = {
  name: '源头厂商名录',

  // 顶栏导航。全站就两个页面，两条即全部；不做「当前页高亮」（原因见 SiteHeader.tsx）
  nav: [
    { href: '/', label: '概览' },
    { href: '/data', label: '名录数据' },
  ],

  /**
   * 页脚导航 = 顶栏两条 + 「数据说明」锚点。
   * 锚点只进页脚不进顶栏：顶栏那两条要维持「这个站有两个页面」的结构感，
   * 混进一个 /data 内部的锚点会把它搅浑。
   */
  footerLinks: [
    { href: '/', label: '概览' },
    { href: '/data', label: '名录数据' },
    { href: '/data#data-source', label: '数据说明' },
  ],

  /**
   * 页脚文字。**只放站上真实存在的事**（站点名、两页入口、由 Excel 推导的条数与
   * 更新日期），**不写公司主体 / 备案号 / 客服电话 / 二维码** ——
   * 本站是纯数据展示页，不承载主体信息，编一个出来就是造假。
   * 页脚里那两个数字由 SiteFooter.tsx 从 getDirectory() 现算，不在这里写死。
   */
  footer: {
    tagline: '把源头厂商的供货记录整理成一张可检索、可排序的表，全部记录取自同一份源文件。',
    // 遮罩这件事在这里必须再说一遍：访客找「怎么联系厂商」时最可能翻到页脚
    masked: '页面上的联系电话与邮件地址均已遮挡，不提供完整联系方式。',
  },
} as const;

export const overview = {
  // 标题是中性数据站标题。本站只展示 Excel 里的名录数据，不承载公司主体信息。
  // 与顶栏品牌、<title> 共用一个值，改这里四处一起变（见上面 site.name）
  title: site.name,
  // 面向访客写的，不要退回成实现说明：「本地」「构建时解析」这类词对访客没有指代对象，
  // 技术细节归 /data 的「数据说明」（见下面 dataSource.notes 第一条）。
  // 「原样呈现」与「不做增删与修饰」原是同一个意思说两遍，已合并。
  // 「原样呈现，不增删、不加工」已删：联系方式被遮罩、价格做了显示格式化，
  // 那句话不再成立。口徑细节仍在 /data 的数据说明里，这里只留一句站得住的结论。
  lead: '源头厂商的供货记录都在这里，取自同一份源文件，记录一条不落；联系方式已做遮挡。',
  // 首页只有概览，名录与数据说明都在 /data，故这两个 href 是跨页路由而非页内锚点。
  // ⚠️ 2026-09-22 起两页之间不再「只有这两条通路」：顶栏与页脚（见上面 site）
  // 也是常驻通路，它们盖住了原来那条「必须同步」的约束。
  actions: [
    { href: '/data', label: '查看名录数据', primary: true },
    { href: '/data#data-source', label: '数据说明', primary: false },
  ],
  /**
   * 品类索引带的标题。这条带子是 2026-09-22 加的，加它是因为：在此之前首页
   * 只有六个数字和两个按钮，**数字是死的、两个按钮还都指向 /data** ——
   * 「落地页 → 我关心的那一类数据」中间没有任何一步。每粒标签是一个
   * 「进 /data 并只看该品类」的深链（拼链接见 lib/tableParams.ts 的 categoryHref），
   * 一步到位。
   *
   * ⚠️ 它不违反「首页只留概览，名录表格在 /data」那条约定：这里给的是**品类索引**，
   * 不是记录；一条供货记录都不在这条带子上。也没有和「查看名录数据」重复 ——
   * 那两件事前面已经因为重复删过一次（见 overview.actions 的注释）。
   */
  categoryTitle: '覆盖品类',
} as const;

export const directory = {
  title: '名录数据',
  // 同样去掉「Excel」这个技术栈名词（口径细节见 dataSource）；「以下」是指向表格的冗余方位词，
  // 换成实指「都在这张表里」，顺带告诉访客表格就在下面。
  // 可做的四件事：2026-09-22 补了「按品类筛选」（见 DirectoryTable.tsx），文案跟着加上。
  lead: '源文件中的全部记录都在这张表里，可按关键词检索、按厂商与品类筛选、点表头排序。',
  emptyText: '数据源中暂无记录。',
} as const;

export const dataSource = {
  title: '数据说明',
  lead: '本页数据的来源、口径与更新方式。',
  // 字段口径逐条对应 src/lib/suppliers.ts 的 SupplierRow，改口径要两处一起改
  fields: [
    { label: '厂商名称', desc: '数据源「源头厂商」列' },
    { label: '品类', desc: '数据源「品类」列，归类以源文件写法为准，站点不另行归并' },
    { label: '供应货品', desc: '数据源「商品」列' },
    { label: '价格', desc: '数据源「价格」列，按该列自带的美元格式 $#,##0.00 展示，未做汇率换算' },
    { label: '联系电话', desc: '数据源「联系方式」列，只展示前 3 位与后 3 位，中间以 * 遮挡' },
    { label: '邮件地址', desc: '数据源「邮件地址」列，@ 前只保留前 3 位，其余以 * 遮挡；域名原样保留' },
    { label: '是否验证', desc: '数据源「是否验证」列，取值与写法均按源文件原样展示' },
  ],
  notes: [
    '数据由本地 Excel 源文件在构建时解析生成，页面不连接任何后端接口。',
    '收录的记录一条不落；如需增删改，请修改源文件后重新构建。',
    '价格按源文件「价格」列自带的美元格式显示（如 $3,999.00），只是显示格式与源文件对齐，数值本身未做汇率换算或任何加工。',
    // 必须说清楚：这是站点对数据源做的**唯一**一处改动，不写出来就等于让访客以为看到的是原值
    '联系电话与邮件地址在构建时即被遮挡：电话只留前 3 位与后 3 位，邮箱只留 @ 前的 3 位（域名保留）。页面与页面源码中都不存在完整的联系方式。',
  ],
} as const;
